# V2 backend API base URL (JWT demo auth + business API)
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# Legacy Neon Auth variables — no longer used by the app code after the
# JWT demo auth migration; safe to delete once nothing else needs them.
# NEON_AUTH_BASE_URL=
# NEON_AUTH_COOKIE_SECRET=
